# CareerCraft Backend Environment
# NOTE: The Anthropic API key is NOT stored here.
# Users provide their own key via the in-app popup.
# This file is only needed for Flask configuration.

FLASK_ENV=development
FLASK_PORT=5000
